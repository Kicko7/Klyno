[phases.setup]
nixPkgs = ["nodejs"]
cmds = ["npm install -g pnpm@9.15.0"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm run start"